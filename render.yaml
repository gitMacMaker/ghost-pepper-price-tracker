services:
  - type: worker
    name: ghost-pepper-price-tracker
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GOOGLE_SERVICE_ACCOUNT_JSON
        sync: false
